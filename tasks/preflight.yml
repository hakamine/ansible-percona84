- name: Ensure required packages are installed (Debian)
  ansible.builtin.apt:
    name:
      - gnupg2
      - lsb-release
    state: present
    update_cache: true
  when: ansible_facts['os_family'] == "Debian"
